<!-- Navigation -->

<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle"
                    data-toggle="collapse" data-target="#example-navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <a class="navbar-brand" href="#">SnowPassion</a>
        </div>

        <div class="collapse navbar-collapse" id="example-navbar-collapse">

            <ul class="nav navbar-nav">
                <li>
                    <a href="{{ path('homepage') }}"><i class="fa fa-home"></i> Accueil</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">

                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}

                        {% if app.user.image is not null %}
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <img class="img-avatar"
                                     src="{{ asset(avatar_directory~app.user.image.id~'.'~app.user.image.ext) }}"
                                     alt="{{ app.user.image.alt }}">
                                <span class="span-avatar">{{ app.user.username }}</span>
                            </a>
                        {% else %}
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <img class="img-avatar"
                                     src="{{ asset('assets/img/avatar_default.png') }}"
                                     alt="default">
                                <span class="span-avatar">Déconnexion</span>
                            </a>
                        {% endif %}

                    {% else %}
                        <a href="{{ path('login') }}">
                            <img class="img-avatar"
                                 src="{{ asset('assets/img/avatar_default.png') }}"
                                 alt="default">
                            <span class="span-avatar">Connexion</span>
                        </a>
                    {% endif %}

                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        <ul class="dropdown-menu">
                            <li>
                                <a href="{{ path('logout') }}">
                                   <span><i class="fa fa-sign-out"></i> Déconnexion</span>
                                </a>
                            </li>
                            <li class="divider"></li>
                            <li><a href="#">
                                    <span><i class="fa fa-cog"></i> Mon compte</span>
                                </a></li>

                        </ul>
                    {% endif %}
                </li>

            </ul>
        </div>
    </div>
</nav>
