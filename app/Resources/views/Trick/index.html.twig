{% extends "base.html.twig" %}

{% block title %}
    Home - {{ parent() }}
{% endblock %}

{% block body %}

    <div class="container">
        <h2>Liste de figures de SnowBoard</h2>
        <hr>
        <div class="col-xs-10 col-xs-offset-1">

            {% if tricks is empty %}
                <i style="font-size:1.8em">Aucune figure disponible</i>
            {% endif %}

            {% for trick in tricks %}
                <div class="media row">
                    <div class="col-md-2">
                        <img src="{{ asset(trick_directory~'grabs.jpg') }}" class="media-object" alt="">
                    </div>
                    <div class="col-md-9">
                        <h4 class="media-heading">{{ trick.nom }}
                            <small>
                                <i>
                                    Publié le {{ trick.date| localizeddate('none', 'none', null, null, 'd MMMM Y') }}
                                </i>
                            </small>
                            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                                <span class="fa fa-pencil"></span>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <a class="delete" data-href="{{ path('delete', {'id': trick.id}) }}">
                                        <span class="fa fa-remove"></span>
                                    </a>
                                {% endif %}
                            {% endif %}
                        </h4>
                        <p>{{ trick.description }} <br><a class="pull-right" href="#">[lire la suite...]</a></p>
                    </div>
                </div>
            {% endfor %}
        </div>

        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            <div class="col-xs-10 col-xs-offset-1">
                <hr>
                <a href="{{ path('homepage') }}" class="btn btn-default pull-right">
                    Ajouter une figure
                </a>
            </div>
        {% endif %}
    </div>
{% endblock %}